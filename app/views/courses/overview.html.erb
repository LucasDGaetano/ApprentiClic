

    <div class="container">
      <h1 class="title-decoration"><strong><%= @course.title %></strong></h1>
      <h2>Pour rappel avant de commencer :</h2>
      <p><%= @course.description %></p>
    </div>

<div class="container">
  <h3 class="my-4 title-decoration">Sommaire</h3>
  <ol>
    <% @chapters.each do |chapter| %>
     <li><h4><%= chapter.title %></h4></li>
    <% end %>
  </ol>
    <hr>

  <div class="my-5">
    <h3 class="title-decoration">Auto-evaluation</h3>
    <p>A la fin de chacun des chapitre affiché ci-dessus vous aurez un petit bouton orange comme ceci :
    "<button class="btn btn-success my-3" style="font-size: 14px;">J'ai compris !</button>"</p>
    <p>Ce bouton sers à <strong> marquer votre progression</strong>, c'est à <strong>vous</strong> d'évaluez si vous avez bien compris un chapitre du cours ! <br> Vous pourrez retrouver votre progression sur votre page de profil après avoir créé un compte</p>
  </div>
<hr>
  <% if user_signed_in? && current_user.users_courses.any? { |uc| uc.course == @course } %>
    <h3>Il semble que vous suivez déjà ce cours</h3>
    <%= link_to "Cliquez ici pour reprendre", course_path(@course), class: "btn btn-success mb-3" %>
  <% else %>
    <h3 class="mt-5">Vous êtes prêt ? On se lance ?</h3>
    <%= simple_form_for [@course, @uc] do |f|%>
    <%= f.submit "Commencer le cours !", class: "btn btn-success mb-5" %>
    <% end %>
  <% end %>
<hr>

  <div data-controller="toggle-comments">
    <button class="btn btn-info" data-action="click->toggle-comments#fire">Voir les commentaires</button>
    <div data-toggle-comments-target="toggable" class="d-none mt-3">
      <h3 class="title-decoration">Ce qu'ils en pensent</h3>
      <% if @course.comments.any? %>
        <div class="my-3 mx-5">
          <h4>Commentaires</h4>
          <% @course.comments.each do |comment| %>
            <div class="my-3">
              <p><strong><%= comment.user.username %></strong> - le <%= comment.created_at.strftime("%d/%m/%Y") %></p>
              <p><%= comment.content %></p>
            </div>
          <% end %>
        </div>
        <% else %>
        <p><em>Ce cours n'a pas encore de commentaires</em></p>
      <% end %>
    </div>
  </div>

  <div>
    <%= link_to "Retourner à la liste de cours", courses_path, class: "btn btn-success my-5" %>
  </div>

</div>
